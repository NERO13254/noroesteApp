async function searchAttributeVehicle(typeOfSearch , append , searchContent) {
    // obtiene el contenedor donde se van a almacenar los datos
    let appendContent = document.getElementById(append);
    // limpia el contenedor 
    appendContent.innerHTML="";
    // realiza la consulta a db
   

}

module.exports ={
    searchAttributeVehicle
} 